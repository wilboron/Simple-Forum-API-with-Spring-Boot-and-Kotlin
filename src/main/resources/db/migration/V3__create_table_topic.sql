CREATE TABLE topic (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
   title VARCHAR,
   message VARCHAR,
   created_at TIMESTAMP WITH TIME ZONE,
   course_id BIGINT,
   author_id BIGINT,
   status VARCHAR,
   CONSTRAINT pk_topic PRIMARY KEY (id)
);

ALTER TABLE topic ADD CONSTRAINT FK_TOPIC_ON_AUTHOR FOREIGN KEY (author_id) REFERENCES users (id);

ALTER TABLE topic ADD CONSTRAINT FK_TOPIC_ON_COURSE FOREIGN KEY (course_id) REFERENCES course (id);