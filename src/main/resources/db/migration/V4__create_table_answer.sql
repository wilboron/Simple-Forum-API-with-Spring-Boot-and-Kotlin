CREATE TABLE answer (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
   message VARCHAR,
   created_at TIMESTAMP WITHOUT TIME ZONE,
   author_id BIGINT,
   topic_id BIGINT,
   solution BOOLEAN,
   CONSTRAINT pk_answer PRIMARY KEY (id)
);

ALTER TABLE answer ADD CONSTRAINT FK_ANSWER_ON_AUTHOR FOREIGN KEY (author_id) REFERENCES users (id);

ALTER TABLE answer ADD CONSTRAINT FK_ANSWER_ON_TOPIC FOREIGN KEY (topic_id) REFERENCES topic (id);